# Pinned to the versions verified in the working development environment
# (Python 3.10/3.11). Regenerate with `pip freeze` after an intentional
# upgrade and re-run the full test suite + a clean-clone rehearsal.

# --- web / API ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
streamlit==1.62.0
altair==6.2.2            # charts (ui/components/charts.py); also a Streamlit dependency
python-multipart==0.0.32 # multipart form parsing for /documents/upload

# --- data layer ---
sqlalchemy==2.0.52
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Groq / LLM ---
groq==0.31.1
langgraph==1.2.11
tenacity==9.1.4

# --- RAG ---
chromadb==1.5.9
sentence-transformers==6.0.1   # local embedding fallback (EMBEDDING_BACKEND=local)
pypdf==6.16.2
python-docx==1.2.0

# --- MCP tool server ---
# Pinned below v2: v2 renamed FastMCP to MCPServer and changed the tool API;
# this project targets the v1 FastMCP stdio surface.
mcp==1.29.1

# --- HTTP / data science ---
httpx==0.28.1
pandas==2.3.3
numpy==2.2.6
xgboost==3.2.0
scikit-learn==1.7.2
joblib==1.6.0

# --- logging ---
structlog==26.1.0

# --- testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
faker==40.37.0
